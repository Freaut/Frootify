<UserControl x:Class="Frootify.SongStatsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Background="#121212">
        <ListView x:Name="SongPlayListView" Background="#1a1a1a" Foreground="DodgerBlue">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Song" DisplayMemberBinding="{Binding Key}" Width="200"/>
                    <GridViewColumn Header="Count" DisplayMemberBinding="{Binding Value}" Width="75"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>

